import type { User, UserSettings } from "../types";
interface ProfileCardProps {
    user: User;
    settings: UserSettings;
}
export declare function ProfileCard({ user, settings }: ProfileCardProps): import("react/jsx-runtime").JSX.Element;
export {};
